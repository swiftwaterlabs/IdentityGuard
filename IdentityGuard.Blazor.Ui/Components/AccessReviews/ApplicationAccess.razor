<Loadable IsLoading="IsLoading">
        @if (IsApplicationLoaded)
        {
            <MudCard>
                <MudCardHeader>
                    <CardHeaderContent>
                        <MudGrid>
                           <MudItem>
                                <MudText Typo="Typo.h5">@ApplicationName</MudText>
                                <MudText Typo="Typo.subtitle2">@DirectoryName</MudText>
                            </MudItem>
                            <MudItem>
                                <MudFab Color="Color.Primary" Size="Size.Small" Icon="@Icons.Material.Filled.Refresh" aria-label="edit" OnClick="RefreshData" />
                            </MudItem>
                        </MudGrid>
                    </CardHeaderContent>
                    <CardHeaderActions>
                    @if (AllowNewAccessReview)
                    {
                        <MudButton Color="Color.Primary" EndIcon="@Icons.Material.Filled.Add" aria-label="start" OnClick="StartReview" Class="mb-2">Start Review</MudButton>
                    }
                   @Actions
                    </CardHeaderActions>
                </MudCardHeader>
                <MudCardContent>
                    <MudTabs Elevation="1" Rounded="true" PanelClass="mt-6">
                        <MudTabPanel Text="General">
                             <AttributeCard Data="UserAttributes"/>
                        </MudTabPanel>
                        <MudTabPanel Text="Owners">
                            <DirectoryObjectCards Data="OwnersByType"/>
                        </MudTabPanel>
                        <MudTabPanel Text="Secrets">
                            <ApplicationSecretCards Data="SecretsByType"/>
                        </MudTabPanel>
                         <MudTabPanel Text="Permissions">
                            <ApplicationPermissionCards Data="PermissionsByResource"/>
                        </MudTabPanel>
                        <MudTabPanel Text="Roles">
                            <RoleCards Data="RolesByType"/>
                        </MudTabPanel>
                    </MudTabs>

                </MudCardContent>
            </MudCard>
        }
    </Loadable>