@page "/user/access/{DirectoryId}/{UserId}"

<MudContainer Class="mt-16">
    <Loadable IsLoading="IsLoading">
        @if (UserAccess != null)
        {
            <MudCard>
                <MudCardHeader>
                    <MudText Typo="Typo.h5">@UserName</MudText>
                    <MudText Typo="Typo.subtitle2">@DirectoryName</MudText>
                    <MudButton Variant="Variant.Filled" Color="Color.Primary" OnClick="StartReview">Start Review</MudButton>
                </MudCardHeader>
                <MudCardContent>
                    <MudTabs Elevation="1" Rounded="true" PanelClass="mt-6">
                        <MudTabPanel Text="General">
                             <IdentityGuard.Blazor.Ui.Components.AttributeCard Data="UserAttributes"/>
                        </MudTabPanel>
                        <MudTabPanel Text="Owned">
                            <MudGrid>
                            @foreach(var type in OwnedObjectsByType.OrderBy(o=>o.Key))
                            {
                                <MudItem xs="12" sm="8" md="4">
                                    <DirectoryObjectGroupCard
                                        Title="@type.Key"
                                        Data ="@type"/>
                                </MudItem>
                            }
                            </MudGrid>
                        </MudTabPanel>
                        <MudTabPanel Text="Groups">
                            <MudGrid>
                            @foreach(var type in GroupMembershipByType.OrderBy(o=>o.Key))
                            {
                                <MudItem xs="12" sm="8" md="4">
                                    <DirectoryObjectGroupCard
                                        Title="@type.Key"
                                        Data ="@type"/>
                                </MudItem>
                            }
                            </MudGrid>
                        </MudTabPanel>
                        <MudTabPanel Text="Roles">
                            <MudText>Show Default Access</MudText>
                            <MudSwitch @bind-Checked="@ShowDefaultAccessRoles" Color="Color.Primary" />

                            <MudGrid>
                                @foreach(var type in ApplicationRolesByAssignmentType.OrderBy(o=>o.Key))
                                {
                                    <MudItem xs="12" sm="8" md="4">
                                        <ApplicationRoleGroupCard 
                                            Title="@type.Key"
                                            Data="@type"
                                            ShowDefaultAccess="ShowDefaultAccessRoles"
                                        />
                                    </MudItem>
                                }
                            </MudGrid>
                        </MudTabPanel>
                    </MudTabs>

                </MudCardContent>
            </MudCard>
        }
    </Loadable>
</MudContainer>